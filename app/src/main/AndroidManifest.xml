<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.remote_suisei_service">

    <application>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Remote_suisei_service.NoActionBar" />

        <service
            android:name=".SuiseiService"
            android:enabled="true"
            android:exported="true"
            android:permission="TODO">
            <intent-filter>

                <!-- バインドの対象となるサービスで提供するインターフェース -->
                <action android:name="com.example.remote_suisei_service.SUISEI_SERVER" />
            </intent-filter>
        </service>
    </application>

</manifest>